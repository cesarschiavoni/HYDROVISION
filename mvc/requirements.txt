# HydroVision AG — MVC TRL 4
# Identidad visual: gestion/doc-11-identidad-visual.md
#
# Frontend (CDN, no requiere pip):
#   Tailwind CSS (CDN) + config override con paleta marca (#1B6B4A, #2D3436, #FAFAF7)
#   Alpine.js 3.x (CDN)
#   Chart.js 3.9.1 (CDN) — pinned, responsive:false + canvas dinámico
#   Leaflet 1.9.4 + Leaflet.Draw 1.0.4 (CDN)
#   Fonts: Inter, IBM Plex Sans, IBM Plex Mono (Google Fonts CDN)

# ── Backend ──────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
sqlalchemy>=2.0.0
pydantic>=2.0.0
jinja2>=3.1.0

# ── Fusión nodo-satélite (opcional, Sentinel-2) ──────────
numpy>=1.24.0
